apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: default
spec:
  interval: 1m
  chart:
    spec:
      chart: podinfo
      version: '0.2.x'
      sourceRef:
        kind: GitRepository
        name: podinfo
        namespace: default
      interval: 1m
  values:
    replicaCount: 2
